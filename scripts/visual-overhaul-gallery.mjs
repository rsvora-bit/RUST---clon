import fs from 'node:fs';
const dir='artifacts/visual-overhaul';
const labels={grassland:'Open grassland',ground:'Ground close-up',coast:'Ocean comparison',beach:'Coastal comparison',upland:'Inland comparison',forest:'Conifer comparison'};
const scenes=[['Coastline daytime','after/coastline-daytime'],['Beach close-up','after/beach-close-up'],['Open grassland','after/grassland'],['Dense forest','qa/dense-forest'],['Forest edge','after/forest-edge'],['Rocky upland','after/rocky-upland'],['Tree close-up','after/tree-close-up'],['Rock close-up','after/rock-close-up'],['Ground close-up','after/ground'],['Ocean close-up','after/ocean-close-up'],['Distant ocean / horizon','after/ocean-horizon'],['Torch at night','qa/torch-night'],['Torch indoors','qa/torch-interior'],['Held gathering item','qa/gathering'],['Building exterior','qa/base-overview'],['Building close-up','qa/building-close-up'],['Building interior after reload','qa/upper-floor-after-reload'],['HIGH overview','after/high-overview'],['Inventory 720p','qa/inventory-720'],['Inventory 1080p','qa/inventory-1080'],['Crafting queue','qa/crafting-queue'],['Building preview','qa/building-preview']];
const figure=(label,path)=>`<figure><a href="${path}.png"><img loading="lazy" src="${path}.png" alt="${label}"></a><figcaption>${label}</figcaption></figure>`;
const pairs=Object.entries(labels).map(([name,title])=>`<section><h2>${title}</h2><div class="pair">${figure('Before',`before/${name}`)}${figure('After',`after/${name}`)}</div></section>`).join('');
const oldPairs=['torch-night','torch-interior','base-overview','gathering'].map(name=>`<section><h2>${name.replaceAll('-',' ')}</h2><div class="pair">${figure('Previous polish pass',`../polish/${name}`)}${figure('Visual overhaul',`qa/${name}`)}</div></section>`).join('');
fs.writeFileSync(`${dir}/index.html`,`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Tideland — visual overhaul</title><style>body{margin:0;background:#151b1c;color:#dce1dc;font:16px system-ui}main{max-width:1600px;margin:auto;padding:32px}h1{font-size:38px;letter-spacing:.08em}h2{font-size:18px;font-weight:500;margin-top:35px}p{max-width:900px;color:#aeb9b6;line-height:1.6}a{color:#b4d7c5}.pair,.grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}figure{margin:0;background:#202829}img{width:100%;display:block}figcaption{padding:12px;color:#b6c4bd}nav{display:flex;gap:24px;margin:28px 0}@media(max-width:800px){.pair,.grid{grid-template-columns:1fr}main{padding:16px}}</style><main><h1>TIDELAND / VISUAL OVERHAUL</h1><p>Original procedural art, captured in the running game. The first six pairs use the same horizontal camera coordinates and viewing targets; camera height follows the terrain. New worlds use generation 2. Legacy saves retain generation 1 terrain and resource placement. Animation and vegetation placement differ between captures.</p><nav><a href="#comparison">Before / after</a><a href="#views">22 representative views</a><a href="after/metrics.json">Performance samples</a><a href="qa/qa-results.json">Gameplay QA</a><a href="qa/settings-profile.json">Settings QA</a></nav><p>This remains procedural game art, not photorealism. Terrain has coherent ridges and valleys; foliage uses alpha cards, structures remain modular timber construction, and the ocean has approximate reflections.</p><div id="comparison">${pairs}${oldPairs}</div><h2 id="views">Representative views</h2><div class="grid">${scenes.map(([label,path])=>figure(label,path)).join('')}</div></main></html>`);
for(const [,path]of scenes)if(!fs.existsSync(`${dir}/${path}.png`))throw Error(`Missing ${path}`);
console.log('Gallery: 22 representative views, 10 comparisons.');
